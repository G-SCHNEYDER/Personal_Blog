<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">

        {# Title #}
        <title>{% block title %}Admin Zone{% endblock %}</title>

        {# Default assets #}
        <link rel="stylesheet" href="{{asset('build/app.css')}}">
        {# Specific per template css #}
        {% block stylesheets %}
        {% endblock %}

    </head>

    {# Main container #}
    <body id="admin_body">

        {% block body %}
        {% endblock %}

        {# Specific per template js #}
        {% block javascripts %}
            {# {{ encore_entry_script_tags('app') }} #}
            <script src="{{asset('build/runtime.js')}}"></script>
            <script src="{{asset('build/vendors-node_modules_symfony_stimulus-bridge_dist_index_js-node_modules_particles_js_particles_js.js')}}"></script>
            <script src="{{asset('build/app.js')}}"></script>
        {% endblock %}
        
    </body>

</html>
